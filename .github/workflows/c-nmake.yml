name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3 
    - name: make
      run: make -f "ha.mak" CFG="ha - Win32 Release"
      working-directory: ./win_msvc
